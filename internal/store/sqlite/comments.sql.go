// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: comments.sql

package sqlite

import (
	"context"
	"database/sql"
)

const createComment = `-- name: CreateComment :one
INSERT INTO comments (public_id, poll_id, participant_id, author_name, body, created_at)
VALUES (?1, ?2, ?3, ?4, ?5, ?6)
RETURNING id, public_id, poll_id, participant_id, user_id, author_name, body, created_at
`

type CreateCommentParams struct {
	PublicID      string
	PollID        int64
	ParticipantID sql.NullInt64
	AuthorName    string
	Body          string
	CreatedAt     string
}

func (q *Queries) CreateComment(ctx context.Context, arg CreateCommentParams) (Comment, error) {
	row := q.db.QueryRowContext(ctx, createComment,
		arg.PublicID,
		arg.PollID,
		arg.ParticipantID,
		arg.AuthorName,
		arg.Body,
		arg.CreatedAt,
	)
	var i Comment
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.PollID,
		&i.ParticipantID,
		&i.UserID,
		&i.AuthorName,
		&i.Body,
		&i.CreatedAt,
	)
	return i, err
}

const deleteComment = `-- name: DeleteComment :exec
DELETE FROM comments WHERE id = ?1 AND poll_id = ?2
`

type DeleteCommentParams struct {
	ID     int64
	PollID int64
}

func (q *Queries) DeleteComment(ctx context.Context, arg DeleteCommentParams) error {
	_, err := q.db.ExecContext(ctx, deleteComment, arg.ID, arg.PollID)
	return err
}

const getComment = `-- name: GetComment :one
SELECT id, public_id, poll_id, participant_id, user_id, author_name, body, created_at FROM comments WHERE id = ?1 AND poll_id = ?2
`

type GetCommentParams struct {
	ID     int64
	PollID int64
}

func (q *Queries) GetComment(ctx context.Context, arg GetCommentParams) (Comment, error) {
	row := q.db.QueryRowContext(ctx, getComment, arg.ID, arg.PollID)
	var i Comment
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.PollID,
		&i.ParticipantID,
		&i.UserID,
		&i.AuthorName,
		&i.Body,
		&i.CreatedAt,
	)
	return i, err
}

const listPollComments = `-- name: ListPollComments :many
SELECT id, public_id, poll_id, participant_id, user_id, author_name, body, created_at FROM comments WHERE poll_id = ?1 ORDER BY created_at, id
`

func (q *Queries) ListPollComments(ctx context.Context, pollID int64) ([]Comment, error) {
	rows, err := q.db.QueryContext(ctx, listPollComments, pollID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Comment
	for rows.Next() {
		var i Comment
		if err := rows.Scan(
			&i.ID,
			&i.PublicID,
			&i.PollID,
			&i.ParticipantID,
			&i.UserID,
			&i.AuthorName,
			&i.Body,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
