// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: participants.sql

package sqlite

import (
	"context"
	"database/sql"
)

const countPollParticipants = `-- name: CountPollParticipants :one
SELECT COUNT(*) FROM participants WHERE poll_id = ?1
`

func (q *Queries) CountPollParticipants(ctx context.Context, pollID int64) (int64, error) {
	row := q.db.QueryRowContext(ctx, countPollParticipants, pollID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createParticipant = `-- name: CreateParticipant :one
INSERT INTO participants (public_id, poll_id, name, email, edit_token_hash, created_at, updated_at)
VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7)
RETURNING id, public_id, poll_id, name, email, user_id, edit_token_hash, created_at, updated_at
`

type CreateParticipantParams struct {
	PublicID      string
	PollID        int64
	Name          string
	Email         sql.NullString
	EditTokenHash string
	CreatedAt     string
	UpdatedAt     string
}

func (q *Queries) CreateParticipant(ctx context.Context, arg CreateParticipantParams) (Participant, error) {
	row := q.db.QueryRowContext(ctx, createParticipant,
		arg.PublicID,
		arg.PollID,
		arg.Name,
		arg.Email,
		arg.EditTokenHash,
		arg.CreatedAt,
		arg.UpdatedAt,
	)
	var i Participant
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.PollID,
		&i.Name,
		&i.Email,
		&i.UserID,
		&i.EditTokenHash,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteParticipant = `-- name: DeleteParticipant :exec
DELETE FROM participants WHERE id = ?1 AND poll_id = ?2
`

type DeleteParticipantParams struct {
	ID     int64
	PollID int64
}

func (q *Queries) DeleteParticipant(ctx context.Context, arg DeleteParticipantParams) error {
	_, err := q.db.ExecContext(ctx, deleteParticipant, arg.ID, arg.PollID)
	return err
}

const getParticipant = `-- name: GetParticipant :one
SELECT id, public_id, poll_id, name, email, user_id, edit_token_hash, created_at, updated_at FROM participants WHERE id = ?1 AND poll_id = ?2
`

type GetParticipantParams struct {
	ID     int64
	PollID int64
}

func (q *Queries) GetParticipant(ctx context.Context, arg GetParticipantParams) (Participant, error) {
	row := q.db.QueryRowContext(ctx, getParticipant, arg.ID, arg.PollID)
	var i Participant
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.PollID,
		&i.Name,
		&i.Email,
		&i.UserID,
		&i.EditTokenHash,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getParticipantByEditTokenHash = `-- name: GetParticipantByEditTokenHash :one
SELECT id, public_id, poll_id, name, email, user_id, edit_token_hash, created_at, updated_at FROM participants WHERE edit_token_hash = ?1
`

func (q *Queries) GetParticipantByEditTokenHash(ctx context.Context, editTokenHash string) (Participant, error) {
	row := q.db.QueryRowContext(ctx, getParticipantByEditTokenHash, editTokenHash)
	var i Participant
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.PollID,
		&i.Name,
		&i.Email,
		&i.UserID,
		&i.EditTokenHash,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listPollParticipants = `-- name: ListPollParticipants :many
SELECT id, public_id, poll_id, name, email, user_id, edit_token_hash, created_at, updated_at FROM participants WHERE poll_id = ?1 ORDER BY created_at, id
`

func (q *Queries) ListPollParticipants(ctx context.Context, pollID int64) ([]Participant, error) {
	rows, err := q.db.QueryContext(ctx, listPollParticipants, pollID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Participant
	for rows.Next() {
		var i Participant
		if err := rows.Scan(
			&i.ID,
			&i.PublicID,
			&i.PollID,
			&i.Name,
			&i.Email,
			&i.UserID,
			&i.EditTokenHash,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateParticipant = `-- name: UpdateParticipant :exec
UPDATE participants SET name = ?1, email = ?2, updated_at = ?3 WHERE id = ?4
`

type UpdateParticipantParams struct {
	Name      string
	Email     sql.NullString
	UpdatedAt string
	ID        int64
}

func (q *Queries) UpdateParticipant(ctx context.Context, arg UpdateParticipantParams) error {
	_, err := q.db.ExecContext(ctx, updateParticipant,
		arg.Name,
		arg.Email,
		arg.UpdatedAt,
		arg.ID,
	)
	return err
}
